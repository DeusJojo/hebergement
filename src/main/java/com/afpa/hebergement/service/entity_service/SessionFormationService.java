package com.afpa.hebergement.service.entity_service;

import com.afpa.hebergement.model.dto.SessionFormationDTO;
import com.afpa.hebergement.service.GenericService;

public interface SessionFormationService extends GenericService<SessionFormationDTO, Integer> {
}
